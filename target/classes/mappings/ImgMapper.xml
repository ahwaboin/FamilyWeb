<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="imgDAO">

	<resultMap type="imgVO" id="imgResultSet">
		<id property="location" column="location" />
		<result property="url" column="url" />
	</resultMap>
	
	<select id="getAll" resultMap="imgResultSet">
		SELECT * FROM backImg
	</select>
	
	<update id="updateUser" parameterType="userVO">
		UPDATE users
		SET
		password=#{password}
		WHERE
		username = #{username}
	</update>
	
	<!-- 
	<delete id="deleteUser" parameterType="userVO">
		DELETE FROM users WHERE
		username = #{username}
	</delete>
	<select id="getSomething" parameterType="int" resultType="#package.#modelname">
		SELECT
		columnname1,
		columnname2,
		columnname3
		FROM tablename1
		WHERE columnname1 = #{value}
	</select>


	<insert id="insertSomething" parameterType="#modelname">
		INSERT INTO tablename1 (columnname1, columnname2, columnname3)
		VALUES(#{value1},#{value2},#{value3})
	</insert>

	 -->

</mapper>
